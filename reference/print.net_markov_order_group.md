# Print method for `net_markov_order_group`

Print method for `net_markov_order_group`

## Usage

``` r
# S3 method for class 'net_markov_order_group'
print(x, ...)
```

## Arguments

- x:

  A `net_markov_order_group` (named list of `net_markov_order` results,
  one per group).

- ...:

  Forwarded to `print.net_markov_order` for each element.

## Value

`x` invisibly.
