% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_inference.R
\name{as.data.frame.net_hon_boot}
\alias{as.data.frame.net_hon_boot}
\title{Coerce a net_hon_boot to its tidy inference table}
\usage{
\method{as.data.frame}{net_hon_boot}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  min_support = NULL,
  order_min = NULL,
  sort_by = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_hon_boot} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{min_support}{Numeric in \verb{[0, 1]} or NULL. Keep only rule edges
with at least this bootstrap support.}

\item{order_min}{Integer or NULL. Keep only rule edges of at least this
order (e.g. \code{2} for the genuinely higher-order rules).}

\item{sort_by}{\code{NULL} (order/from/to, default), \code{"count"},
\code{"probability"}, or \code{"support"} - sort the table by that column,
largest first (ties broken by from/to so the order is deterministic).}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame, one row per rule edge: \code{from}, \code{to}, \code{order},
\code{count}, \code{probability}, \code{ci_lower}, \code{ci_upper}, \code{support},
\code{n_boot_used}.
}
\description{
Coerce a net_hon_boot to its tidy inference table
}
