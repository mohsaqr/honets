% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_inference.R
\name{as.data.frame.net_hon_compare}
\alias{as.data.frame.net_hon_compare}
\title{Coerce a net_hon_compare to its tidy edge table}
\usage{
\method{as.data.frame}{net_hon_compare}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  significant = FALSE,
  sort_by = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_hon_compare} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{significant}{Logical. \code{TRUE} restricts to edges whose adjusted
p-value falls below the object's \code{alpha}. Default \code{FALSE} (all
edges).}

\item{sort_by}{\code{NULL} (order/from/to, default), \code{"abs_diff"},
\code{"count"}, or \code{"p_adj"} - sort by absolute difference or count
(largest first) or adjusted p-value (smallest first), ties broken by
from/to.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame, one row per pooled rule edge (see
\code{\link[=compare_hon]{compare_hon()}} for the columns).
}
\description{
Coerce a net_hon_compare to its tidy edge table
}
