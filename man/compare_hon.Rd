% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hon_inference.R
\name{compare_hon}
\alias{compare_hon}
\title{Two-sample permutation comparison of higher-order network rules}
\usage{
compare_hon(
  x,
  y,
  n_perm = 1000L,
  alpha = 0.05,
  max_order = 5L,
  min_freq = 1L,
  collapse_repeats = FALSE,
  action = NULL,
  actor = NULL,
  time = NULL,
  names = c("x", "y"),
  parallel = FALSE,
  n_cores = 2L,
  seed = NULL
)
}
\arguments{
\item{x, y}{The two cohorts of sequence data, each in any input format
accepted by \code{\link[=bootstrap_hon]{bootstrap_hon()}} (wide data.frame, list,
\code{tna}/\code{netobject}, or long data.frame with \code{action}/\code{actor}/\code{time}).}

\item{n_perm}{Integer >= 2. Label permutations. Default \code{1000}.}

\item{alpha}{Significance level for the \code{significant} flag on the
adjusted p-values. Default \code{0.05}.}

\item{max_order, min_freq, collapse_repeats}{As in \code{\link[=build_hon]{build_hon()}}.}

\item{action, actor, time}{Long-format column names applied to both \code{x}
and \code{y}; \code{NULL} for wide/list input.}

\item{names}{Character vector of length 2 naming the cohorts in the
output (default \code{c("x", "y")}).}

\item{parallel, n_cores, seed}{As in \code{\link[=bootstrap_hon]{bootstrap_hon()}}.}
}
\value{
An object of class \code{net_hon_compare}: a list with \code{edges} (one
row per pooled rule edge: \code{from}, \code{to}, \code{order}, \code{count},
\code{count_x}/\code{count_y} and \code{prob_x}/\code{prob_y} (columns named after
\code{names}), \code{diff} (prob difference, first minus second), \code{p_value},
\code{p_adj} (BH), \code{significant}, \code{n_perm_used}), \code{global} (list:
\code{statistic} - the pooled-count-weighted mean absolute difference -
and \code{p_value}), \code{names}, \code{n_perm}, \code{alpha}, \code{max_order}, \code{min_freq},
\code{n_trajectories} (per cohort), and \code{seed}. Has \code{print}, \code{summary},
\code{plot}, and \code{as.data.frame} methods; \code{as.data.frame()} returns the
edge table (\code{significant = TRUE} restricts it).
}
\description{
Tests whether two cohorts of sequences differ in their higher-order
rule probabilities. The rule set is extracted from the pooled data (see
\code{\link[=build_hon]{build_hon()}}); for every pooled rule edge the statistic is the
absolute difference of the two cohorts' conditional probabilities, and
its null distribution comes from permuting cohort labels over
sequences. Per-edge p-values are Benjamini-Hochberg adjusted; a global
test aggregates the edge differences weighted by pooled counts.
}
\details{
As in \code{\link[=bootstrap_hon]{bootstrap_hon()}}, per-sequence counts are precomputed once and
every permutation is a weighted aggregation; permutations are drawn
before any parallel work, so \code{parallel = TRUE} reproduces the serial
result under the same \code{seed}. Edges whose context is unobserved in a
cohort under some permutation contribute only their valid permutations
(\code{n_perm_used}).
}
\examples{
first_order  <- replicate(6, sample(c("a", "b", "c"), 12, replace = TRUE),
                          simplify = FALSE)
second_order <- replicate(6, rep(c("a", "b", "c", "b"), 3),
                          simplify = FALSE)
cmp <- compare_hon(first_order, second_order, n_perm = 99,
                   max_order = 2, seed = 1)
cmp
head(as.data.frame(cmp))

}
\references{
Xu, J., Wickramarathne, T. L., & Chawla, N. V. (2016). Representing
higher-order dependencies in networks. \emph{Science Advances} 2(5),
e1600028. \doi{10.1126/sciadv.1600028}

Good, P. (2005). \emph{Permutation, Parametric and Bootstrap Tests of
Hypotheses} (3rd ed.). Springer.
}
\seealso{
\code{\link[=bootstrap_hon]{bootstrap_hon()}}, \code{\link[=build_hon]{build_hon()}}, \code{\link[=markov_order_test]{markov_order_test()}}
}
