% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_mogen.R
\name{as.data.frame.net_mogen}
\alias{as.data.frame.net_mogen}
\title{Coerce a net_mogen to a tidy table}
\usage{
\method{as.data.frame}{net_mogen}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("orders", "transitions"),
  order = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_mogen} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{what}{\code{"orders"} (default) for the per-order model-selection table,
or \code{"transitions"} for the fitted transition probabilities (the same
table \code{\link[=mogen_transitions]{mogen_transitions()}} returns).}

\item{order}{Integer or \code{NULL}. Only for \code{what = "transitions"}: which
layer's transitions to return. Default \code{NULL} uses the selected order.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame. For \code{what = "orders"}, one row per candidate order:
\code{order}, \code{log_likelihood}, \code{aic}, \code{bic}, \code{dof}, \code{layer_dof}, and
\code{optimal} (logical, \code{TRUE} for the selected order). For
\code{what = "transitions"}, one row per transition.
}
\description{
Coerce a net_mogen to a tidy table
}
