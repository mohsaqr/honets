% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_laplacian.R
\name{plot.net_hypergraph_cluster}
\alias{plot.net_hypergraph_cluster}
\title{Plot method for net_hypergraph_cluster}
\usage{
\method{plot}{net_hypergraph_cluster}(x, what = c("both", "spectrum", "embedding"), n_values = NULL, ...)
}
\arguments{
\item{x}{A \code{net_hypergraph_cluster} object.}

\item{what}{Character. \code{"both"} (default), \code{"spectrum"}, or
\code{"embedding"}.}

\item{n_values}{Integer. How many smallest eigenvalues to show in the
spectrum panel (default: \code{min(3 * k, n_nodes)}).}

\item{...}{Additional arguments (ignored).}
}
\value{
For \code{"spectrum"}/\code{"embedding"}, the ggplot object. For
\code{"both"}, the arranged gtable when gridExtra is installed (drawn on
the current device), otherwise the two panels are drawn via grid
viewports and the list of the two ggplots is returned invisibly.
}
\description{
Two diagnostic panels. \code{"spectrum"}: scree plot of the Laplacian
spectrum with the k used for clustering marked - the eigengap after k
supports (or questions) the choice of k. \code{"embedding"}: the nodes in
the first two spectral-embedding dimensions, labelled, coloured and
shaped by cluster, sized by stationary probability - the geometry
k-means actually clustered. \code{"both"} (default) arranges the two side
by side (via gridExtra when available, base grid viewports otherwise).
}
