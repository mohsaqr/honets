% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplicial.R
\name{as.data.frame.net_simplicial}
\alias{as.data.frame.net_simplicial}
\title{Coerce a net_simplicial to a tidy table}
\usage{
\method{as.data.frame}{net_simplicial}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("simplices", "f_vector"),
  dim = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_simplicial} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{what}{\code{"simplices"} (default) for one row per simplex, or
\code{"f_vector"} for the face counts by dimension.}

\item{dim}{Integer or \code{NULL}. Only for \code{what = "simplices"}: keep only
simplices of this dimension (\code{0} vertices, \code{1} edges, \code{2} triangles).}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame. For \code{what = "simplices"}, one row per simplex:
\code{id}, \code{dim}, \code{size}, \code{members} (the node names, comma separated). For
\code{what = "f_vector"}, one row per dimension: \code{dim}, \code{count}.
}
\description{
Coerce a net_simplicial to a tidy table
}
