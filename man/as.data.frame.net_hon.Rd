% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_hon.R
\name{as.data.frame.net_hon}
\alias{as.data.frame.net_hon}
\title{Coerce a net_hon to a tidy table}
\usage{
\method{as.data.frame}{net_hon}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("rules", "nodes"),
  order_min = NULL,
  sort_by = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_hon} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{what}{\code{"rules"} (default) for the extracted higher-order rules, or
\code{"nodes"} for the node table of the higher-order topology.}

\item{order_min}{Integer or \code{NULL}. Keep only rules whose source context
is at least this order (e.g. \code{2} for the genuinely higher-order rules).
Ignored when \code{what = "nodes"}.}

\item{sort_by}{\code{NULL} (construction order, default), \code{"count"} or
\code{"probability"} - sort largest first, ties broken by \code{from}/\code{to} so the
order is deterministic. Ignored when \code{what = "nodes"}.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame. For \code{what = "rules"}, one row per rule edge with
columns \code{path}, \code{from}, \code{to}, \code{count}, \code{probability}, \code{from_order},
\code{to_order}. For \code{what = "nodes"}, one row per higher-order node with
columns \code{id}, \code{label}, \code{name}.
}
\description{
Coerce a net_hon to a tidy table
}
