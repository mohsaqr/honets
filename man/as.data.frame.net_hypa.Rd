% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_hypa.R
\name{as.data.frame.net_hypa}
\alias{as.data.frame.net_hypa}
\title{Coerce a net_hypa to a tidy table}
\usage{
\method{as.data.frame}{net_hypa}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("scores", "over", "under"),
  sort_by = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_hypa} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{what}{\code{"scores"} (default) for every scored path, \code{"over"} for the
significantly over-represented paths only, or \code{"under"} for the
significantly under-represented ones.}

\item{sort_by}{\code{NULL} (construction order, default), \code{"p"} (sorts by
\code{p_value}, smallest first), \code{"ratio"} (largest observed/expected
deviation first) or \code{"observed"} (heaviest paths first). \code{p_value} is
the two-sided value; the direction-specific \code{p_adjusted_under} /
\code{p_adjusted_over} columns are left for the caller to read, since a
single ordering cannot serve both directions.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame, one row per path: \code{path}, \code{from}, \code{to},
\code{observed}, \code{expected}, \code{ratio}, the p-values (\code{p_value}, \code{p_under},
\code{p_over}, \code{p_adjusted_under}, \code{p_adjusted_over}), the \code{anomaly} label
and the path \code{order}.
}
\description{
Coerce a net_hypa to a tidy table
}
