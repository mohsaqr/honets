% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplicial_homology.R
\name{as.data.frame.net_persistent_homology}
\alias{as.data.frame.net_persistent_homology}
\title{Coerce a net_persistent_homology to a tidy table}
\usage{
\method{as.data.frame}{net_persistent_homology}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("persistence", "betti"),
  dimension = NULL,
  sort_by = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_persistent_homology} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{what}{\code{"persistence"} (default) for the persistence diagram, or
\code{"betti"} for the Betti curves across the filtration.}

\item{dimension}{Integer or \code{NULL}. Keep only this homological dimension.}

\item{sort_by}{\code{NULL} (construction order, default) or \code{"persistence"} -
longest-lived features first. Only for \code{what = "persistence"}.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame. For \code{what = "persistence"}, one row per feature:
\code{dimension}, \code{birth}, \code{death}, \code{persistence}. For \code{what = "betti"}, one
row per (threshold, dimension): \code{threshold}, \code{dimension}, \code{betti}.
}
\description{
Coerce a net_persistent_homology to a tidy table
}
