% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_markov_order.R
\name{as.data.frame.net_markov_order}
\alias{as.data.frame.net_markov_order}
\title{Coerce a net_markov_order to a tidy table}
\usage{
\method{as.data.frame}{net_markov_order}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("orders", "null"),
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_markov_order} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{what}{\code{"orders"} (default) for the per-order test table, or
\code{"null"} for the permutation null distribution behind it.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame. For \code{what = "orders"}, one row per candidate order
with the log-likelihood, information criteria, likelihood-ratio
statistic and permutation p-value. For \code{what = "null"}, one row per
permutation replicate: \code{order}, \code{replicate}, \code{g2} (the replicate's
likelihood-ratio statistic under the null).
}
\description{
Coerce a net_markov_order to a tidy table
}
