% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_measures.R
\name{as.data.frame.net_hypergraph_measures}
\alias{as.data.frame.net_hypergraph_measures}
\title{Coerce a net_hypergraph_measures to a tidy table}
\usage{
\method{as.data.frame}{net_hypergraph_measures}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("nodes", "edges", "global"),
  sort_by = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_hypergraph_measures} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{what}{\code{"nodes"} (default) for the per-node measures, \code{"edges"} for
the per-hyperedge measures, or \code{"global"} for the whole-hypergraph
summary.}

\item{sort_by}{\code{NULL} (node/edge order, default), or a column name to
sort by, largest first. Ignored when \code{what = "global"}.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame. For \code{what = "nodes"}, one row per node: \code{node},
\code{hyperdegree}, \code{node_strength}, \code{max_edge_size}. For \code{what = "edges"},
one row per hyperedge: \code{hyperedge}, \code{size}. For \code{what = "global"}, one
row per statistic: \code{measure}, \code{value}.
}
\description{
Coerce a net_hypergraph_measures to a tidy table
}
