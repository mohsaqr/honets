% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_path_dependence.R
\name{as.data.frame.net_path_dependence}
\alias{as.data.frame.net_path_dependence}
\title{Coerce a net_path_dependence to its tidy per-context table}
\usage{
\method{as.data.frame}{net_path_dependence}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  min_count = NULL,
  sort_by = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_path_dependence} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{min_count}{Integer or \code{NULL}. Keep only contexts observed at least
this many times.}

\item{sort_by}{\code{NULL} (construction order, default) or \code{"KL"} - sort by
the order-k vs order-1 divergence, largest first.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame, one row per context, carrying the context label,
its observed count, and the Kullback-Leibler divergence between its
order-k and order-1 continuation distributions.
}
\description{
Coerce a net_path_dependence to its tidy per-context table
}
