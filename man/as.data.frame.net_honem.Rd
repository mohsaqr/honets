% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_honem.R
\name{as.data.frame.net_honem}
\alias{as.data.frame.net_honem}
\title{Coerce a net_honem to a tidy table}
\usage{
\method{as.data.frame}{net_honem}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("embeddings", "variance"),
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_honem} object.}

\item{row.names}{Ignored (S3 consistency).}

\item{optional}{Ignored (S3 consistency).}

\item{...}{Additional arguments (ignored).}

\item{what}{\code{"embeddings"} (default) for the node coordinates, or
\code{"variance"} for the per-dimension singular values.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame. For \code{what = "embeddings"}, one row per higher-order
node: \code{node} followed by one column per embedding dimension (\code{dim1},
\code{dim2}, ...). For \code{what = "variance"}, one row per dimension: \code{dim},
\code{singular_value}, \code{proportion} (that dimension's share of the total
squared singular value).
}
\description{
Coerce a net_honem to a tidy table
}
