% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplicial.R
\name{simplicial_degree}
\alias{simplicial_degree}
\title{Simplicial Degree}
\usage{
simplicial_degree(sc, normalized = FALSE, top = NULL)
}
\arguments{
\item{sc}{A \code{net_simplicial} object.}

\item{normalized}{Divide by maximum possible count. Default \code{FALSE}.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
Data frame with \code{node}, columns \code{d0} through
\code{d_k}, and \code{total} (sum of d1+). Sorted by total descending.
}
\description{
Counts how many simplices of each dimension contain each node.
}
\examples{
mat <- matrix(c(0,.6,.5,.6,0,.4,.5,.4,0), 3, 3)
colnames(mat) <- rownames(mat) <- c("A","B","C")
sc <- build_simplicial(mat, threshold = 0.3)
simplicial_degree(sc)

}
